[package]
name = "kora-syntax"
version.workspace = true
edition.workspace = true
license.workspace = true
# Not published to crates.io: the crates depend on each other by path.
publish = false

[dependencies]
